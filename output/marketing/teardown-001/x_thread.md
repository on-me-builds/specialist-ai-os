# X thread draft — Teardown #001 — Product Factory v0.4: one YAML in, four deliverables out

1. Teardown #001 — Product Factory v0.4: one YAML in, four deliverables out — notes from a hands-on teardown. Key takeaways in this thread.
2. One clinic profile YAML in, four Markdown deliverables out, regenerated in seconds
3. A validation gate must PASS before any output is treated as usable
4. LLM-assisted drafts fall back to plain templates when no API key is set
5. Input and output stay fully separated so an orchestrator can be added later
6. Every generated document stays a draft until a human reviews it
7. Full teardown is in the repo. Corrections and critique welcome.

---

**Boundary (do not remove)**

Draft only. Human review is required before this text is used anywhere.
This is not a product launch, not a safety guarantee, and not a security claim.
Generated locally. Nothing here is published or sent by any system; a human
must explicitly approve and perform every external action.
